name: arviointi_app
description: "Offline-first arviointi-appi (MVP: local DB + evalpack import/export/share)"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  app_links: ^6.4.0
  url_launcher: ^6.3.0
  

  flutter_riverpod: ^2.5.1
  drift: ^2.20.0
  drift_flutter: ^0.2.0
  path_provider: ^2.1.4
  path: ^1.9.0

  uuid: ^4.4.2
  crypto: ^3.0.3
  archive: ^3.6.1

  file_picker: ^8.1.2
  share_plus: ^10.0.2

  receive_sharing_intent: ^1.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  drift_dev: ^2.20.0
  build_runner: ^2.4.12
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

